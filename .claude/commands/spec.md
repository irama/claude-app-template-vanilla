Read PROJECT_SPEC.md and the current codebase, then report:

1. **Built so far** — which spec features are fully implemented
2. **In progress** — which are partially implemented
3. **Not started** — which haven't been touched yet
4. **Deviations** — anything in the codebase that differs from the spec (and whether that's intentional)
5. **Blockers** — any spec requirements that seem unclear or contradictory

Keep it concise. Use a checklist format for items 1–3.
